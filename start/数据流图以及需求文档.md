希望实现的功能
1.为什么叫start文件夹,因为这里希望在设计主角名字哪些登记
![alt text](QQ_1785486393941.png)
就是这里的开始页面路由不应该再路由游戏游玩，并且现在也该为存档结构定性

---

## 需求确认（2026-08-01，Reasonix 测试岗记录，待人工修改与配图）

> 本节为需求确认草稿：布局以 `QQ_1785486393941.png` 与参考视频为准（Reasonix 无法看图），图片由人工补入；字段清单以心跳回忆 4 素材为准。

### 已确认决策

| 项 | 决策 |
| --- | --- |
| 注册界面形态 | GAL 演出页（心跳回忆 4 式），具体布局看图/视频 |
| 入口行为 | `restart` 与首次进入 → 玩家注册页；`continue` 读档跳过注册（无档时 continue 已禁用） |
| 字段范围 | 心跳回忆 4 全套字段（名字/生日/血型/兴趣/部活等，以素材为准） |
| 存档结构 | schema v3：玩家信息定入 `GameSnapshot`（当前 v2，`save/snapshot.ts:32`） |
| 改名规则 | **注册后名字固定**，不可改（名字是楼层配对签名的一部分，`message/protocol.ts:141`） |
| cardStore | 本轮顺带清理（脏代码） |

### 数据链路（玩家信息）

```
playerStore（内存，PlayerState）
  ├─► createGameSnapshot()  save/snapshot.ts:146-157   → GameSnapshot.player
  │     └─► savesolt write() → /api/files/upload       → Tavern user files 存档槽
  ├─► createSavePreview()    save/snapshot.ts:166-175   → 槽位预览 playerName
  └─► restoreGameSnapshot()  save/snapshot.ts:177-212   → 校验后写回 playerStore

名字消费方（注册后自动生效，无需改）：
  GalMainStory.tsx:69（AI 请求 playerName）、storyPresentation.ts:52（说话人归一化）、
  tavernStoryGeneration.ts:95（prompt）、index.tsx:143（render_game_to_text）、
  message/protocol.ts:99,141（楼层元数据+配对签名）、summaryPrompts.ts:204、storyPersistence.ts:96
```

### 待大哥们确认/实现

1. 注册页布局：以 `QQ_1785486393941.png` + 参考视频为准
2. 字段清单最终定义：以心跳回忆 4 素材为准（素材路径：____待人工填写____）
3. 世界书 `User` → 玩家名替换（若本轮做）：注入点 `data/storyLore.ts:196-201`（`WORLDINFO_ENTRIES_LOADED` 回调改扫描副本 content，原件不动），需真实酒馆人工核对
4. 说话人归一化后续改为从角色卡拉取角色名（`storyPresentation.ts:53` 目前查内置静态表）

### 测试验收点（Reasonix 自动化 + 人工）

| # | 验收点 | 方式 |
| --- | --- | --- |
| 1 | 存档 v3 契约：新字段/类型/范围校验断言 | 自动（verify 脚本） |
| 2 | 路由流转：`restart → 注册 → 完成 → game`；注册未完成不得进游戏；`continue` 跳过注册 | 自动（mock store） |
| 3 | 名字固定：注册后无改名路径；历史消息签名与新档名字一致 | 自动 |
| 4 | 名字消费方回归：prompt / render_game_to_text / GAL 归一化用新名字 | 自动 + 人工 |
| 5 | 世界书 User 替换：扫描副本替换、Tavern 原件不动 | 人工（真实酒馆） |
| 6 | cardStore 清理回归（见下） | 自动 + 人工 |

### cardStore 清理回归面

| 消费方 | 位置 |
| --- | --- |
| CharacterArchivePanel / CharacterPortrait / ClassroomScene / Controls | 4 个组件 |
| snapshot（快照/恢复）、autosave（订阅 :221）、characterPresence、gameSession | 4 个服务 |
| characterStore（镜像 + 订阅 :119）、index.tsx（render_game_to_text） | 2 个桥 |

